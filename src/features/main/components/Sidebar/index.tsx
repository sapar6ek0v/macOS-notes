import ListItem from '../ListItem';
import { SidebarWrapper } from './styles';

const SideBar = () => {
  return (
    <SidebarWrapper>
      <ListItem />
    </SidebarWrapper>
  );
};

export default SideBar;
